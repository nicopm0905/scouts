# CHANGES.md — Migraciones aditivas y cambios al núcleo

Registro de cambios que los agentes de feature hacen sobre el esquema/modelos compartidos.
**No editar migraciones del núcleo.** Cualquier campo nuevo va en una migración aditiva y se
documenta aquí.

| Fecha | Agente | Migración / cambio | Motivo | Afecta a |
|-------|--------|--------------------|--------|----------|
| 2026-09-07 | Formato MSC (retirada del guion) | Sin migraciones. **Eliminado el PDF de guion de sesión**: ruta `events.pdf.session` (`/eventos/{event}/pdf/guion`), `EventPdfController@session`, la vista `resources/views/pdf/event-session.blade.php`, su enlace en `Events/Show` y su test. | La ficha de salida MSC cubre lo mismo con el formato oficial de la delegación, así que el guion duplicaba trabajo | Quien enlazase a `events.pdf.session` (ya no existe la ruta) |
| 2026-09-07 | Formato MSC (enlaces y robustez) | Sin migraciones. `MscOutingSheet` ya no pierde actividades: la franja acepta la clave del catálogo y la etiqueta escrita a mano, el día se empareja sin tildes ni mayúsculas y admite "Día 2" o una fecha, lo no reconocido cae en la primera columna y lo que no tiene franja se devuelve en `unplaced` (se imprime al pie de la ficha). `EventController@show` añade props `availableActivities` y `mscReadiness`, y `activities` pasa a llevar `type_label`, `owner`, `time_slot_label`, `objectives[]` (con ámbito y objetivo) y `missing[]`. `Events/Show.vue` sustituye el cronograma por la sección "Actividades y objetivos del plan" con enlazar/quitar reutilizando `activities.events.attach|detach`. Seeder nuevo `MscDemoSeeder`. | La ficha salía en blanco si no se enlazaba nada y no había forma de enlazar desde el evento; además una tilde en el día borraba la actividad del impreso | Quien dependa del prop `activities` de `Events/Show` |
| 2026-09-07 | Formato MSC (plan de rama y salidas) | Migración aditiva `add_msc_plan_format_fields`: `branch_plan_objectives` gana `scope`, `line`, `current_situation`, `goal_verb`, `goal_complement` y `evaluation`; `activities` gana `activity_type`, `owner`, `scheduled_date`, `place` y `evaluation` (todos en fillable/casts). Enums nuevos `MscScope` (Responsabilidad/País/Fe, con color de impresión) y `ActivityType`. Catálogo `App\Support\MscPlanCatalog` con las líneas y contenidos oficiales por ámbito, los verbos de objetivo y las franjas horarias del impreso (mañana 10-14, tarde I 16-18, tarde II 19-21, noche 22-23); `forFrontend()` alimenta los desplegables encadenados. `BranchPlanObjective::goalText()` compone "verbo + complemento" y cae en `description` para los objetivos anteriores; `Store/UpdateObjectiveRequest` rellenan `description` sola en `prepareForValidation`. Servicios nuevos `App\Services\Plans\TermPlanSheet` y `App\Services\Events\MscOutingSheet`. Rutas nuevas `branch-plans.pdf.term` (`/branch-plans/{plan}/pdf/trimestre/{term}`) y `events.pdf.msc-outing` (`/eventos/{event}/pdf/ficha-salida`) con sus vistas `pdf/branch-plan-term.blade.php` y `pdf/event-msc-outing.blade.php`. **Ojo:** `activities.time_slot` pasa a guardar las claves del catálogo (`manana`, `tarde_1`, `tarde_2`, `noche`); las fichas antiguas con el texto en claro ("Mañana", "Tarde I"…) se siguen leyendo, pero al reeditarlas hay que volver a elegir la franja. | Que el plan de rama y las salidas se rellenen y se impriman con el formato de la Delegación Diocesana del MSC, el mismo que el kraal usaba en la hoja de cálculo y en el impreso de campada (ROADMAP Épica F) | Plan de rama, biblioteca de actividades y PDFs de evento |
| 2026-09-07 | Formato MSC (impresos) | Sin migraciones. Membrete oficial extraído a `resources/views/pdf/partials/msc-letterhead.blade.php` (logo del grupo centrado, sellos de Andalucía y MSC, texto legal girado 90º en el margen izquierdo y pie de la delegación), todo con `position: fixed` para repetirse en cada página. Acepta `$mscLandscape` para omitir el texto vertical y los sellos en apaisado. **La hoja que lo incluya tiene que reservar el hueco en `@page`**: ~53mm arriba, 28mm a la izquierda y 15mm abajo en vertical. `pdf/event-msc-outing.blade.php` y `pdf/branch-plan-term.blade.php` reescritas para calcar el impreso: colores de banda por ámbito, etiquetas naranjas, desplegables dibujados, cuatro filas fijas por ámbito y rejilla ESTRUCTURA con las horas por defecto. | Que los PDF salgan idénticos al impreso en papel de la delegación, como ya hacía la autorización | Cualquier PDF nuevo que deba llevar el membrete del grupo |
| 2026-09-06 | Fase 5 · retención | Migración aditiva `add_left_at_to_members_table` (`left_at` date en `members`, en fillable/casts). `Member::booted()` con hook `saving`: al pasar `active` a false rellena `left_at` (si estaba vacía), al volver a true la limpia — cubre todos los orígenes (edición de ficha, renovación de curso, importador). `AnnualReportService` añade bloque `retention` (altas por `joined_at`, bajas por `left_at`, neto) a la memoria; se pinta en `Reports/AnnualReport.vue` y en `pdf/annual-report.blade.php`. | Medir altas/bajas por curso sin campo de baja previo (ROADMAP Fase 5) | Ficha de miembro (columna nueva); memoria del curso |
| 2026-09-06 | Fase 4 · Épica G (cierre de curso) | Migración aditiva `add_census_fields_to_members_table` (`dni`, `sex` `M/F/X`, `address` en `members`; en fillable de `Member`, en `Store/UpdateMemberRequest`, en `MemberForm.vue` + `memberDetail()`). **Memoria del curso**: `App\Services\Reports\AnnualReportService` + `Reports\AnnualReportController` (`/memoria`, `/memoria/pdf`, permiso `plans.view`, alcance por rama) + `Pages/Reports/AnnualReport.vue` + `pdf/annual-report.blade.php` + entrada de menú "Memoria del curso". **Presupuesto vs real**: `Budget::summary()` (previsto/real/desvío); `FinanceReportService` añade `budgets[]` al informe y filas al CSV; `Finance/Report.vue` tabla nueva; `BudgetController@show` pasa `comparison` y `Budgets/Show.vue` muestra el desvío. **Censo MSC**: `MemberController@censusMsc` (`/members/censo-msc`) + botón en `Members/Index`. | Cierre de curso: memoria anual, control presupuestario y censo de la federación (ROADMAP Fase 4 · Épica G) | Ficha de miembro (3 columnas nuevas); tesorería; secretaría/coordinación |
| 2026-09-06 | Fase 4 · Épica E (revisión de datos) | Migración aditiva `create_member_change_requests_table` + enum `ChangeRequestStatus` + modelo `MemberChangeRequest` (`payload` json, `member()/submitter()/reviewer()`, `scopePending`, `apply()` / `reject()`). **Ojo:** la columna se llama `payload` (no `changes`) porque `changes` colisiona con la propiedad interna `$changes` de Eloquent. La familia envía propuestas desde `Portal\ChildController@submitReview` (`POST /portal/scouts/{member}/revision`); secretaría revisa en `Members\ChangeRequestController` (`/revisiones-familias`, permiso `members.manage`, alcance por rama vía `MemberPolicy@update`). **Ampliado el mismo día**: el `payload` tiene grupos `member` (phone/email), `health` (solo con `members.sensitive`), `family` (`Family.contact_phone/email` de la familia de la cuenta), `consents` (RGPD/imagen/salidas → `updateOrCreate` en `consents` con `signed_at`), `renewal` (`continues` false → `Member.active=false`). `apply()` los aplica en una transacción; `ChangeRequestController@diff` muestra el antes→después de todos. `DashboardController@attention` añade aviso `family-reviews`. `AppLayout` + `DashIcon`: icono `inbox` nuevo. | Que las familias mantengan sus datos al día y respondan la campaña de inicio de curso sin que secretaría teclee (ROADMAP Fase 4 · Épica E) | Portal de familias; secretaría; quien dependa de props del Dashboard |
| 2026-09-06 | Fase 3 · Asistente de salida | Migración aditiva `add_assignment_to_event_checklist_items_table` (`assigned_to` FK→users nullOnDelete, `due_at` date; ambos en fillable/casts de `EventChecklistItem`, relación `assignee()`). `EventChecklistController@store` y `Store/UpdateChecklistItemRequest` aceptan `assigned_to`/`due_at`. `EventController@show` añade props `preparation` (estado de los 8 pasos para preparar una salida) y `staffUsers`; `UserRole::staffValues()` nuevo. `DashboardController` añade prop `tasks` (tareas de kraal asignadas al usuario, sin cerrar). Nuevo endpoint `POST /cobros/{charge}/marcar-pagados` (`charges.members.bulk-mark-paid`, `ChargeController@bulkMarkPaid` + `BulkMarkChargeMembersPaidRequest`) para cerrar varios pagos en efectivo de una vez. | Encadenar la preparación de una salida en una pantalla y cerrar cobros en efectivo en bloque (ROADMAP Épica C/D) | Quien dependa de los props del Dashboard o de `Events/Show`; nuevo campo en `event_checklist_items` |
| 2026-09-06 | Correo (ajuste) | Sin migraciones. `.env` con SMTP de Gmail activo para correo transaccional (recordatorios de pago, avisos de caducidad, firma de autorizaciones). Las **invitaciones de acceso ya no envían correo**: `Admin\UserController@store/resendInvite` y `FamilyAccountController@invite` generan el token y devuelven `session('invite_url')`; coordinación entrega el enlace a mano. El flujo self-service "¿olvidaste tu contraseña?" del login sigue enviando correo. | El grupo prefiere no mezclar las altas con el correo oficial | Coordinación |
| 2026-09-06 | Gestión de usuarios + correo | Migración aditiva `add_last_login_at_to_users_table` (`last_login_at` en fillable/casts de `User`; listener de `Login` en `AppServiceProvider` lo actualiza con `saveQuietly`). Permiso nuevo `users.manage` (solo coordinación). `UserPolicy` (viewAny/create/update por `users.manage`; las salvaguardas "último admin" y "no bloquearse a uno mismo" van en el controlador porque `Gate::before` haría que el admin se saltase la policy). Pantalla **Usuarios** (solo admin): `Admin\UserController` (index/store/update/resendInvite/destroy) + `Requests\Admin\{Store,Update}UserRequest` + `routes/features/users.php` + `Pages/Admin/Users/Index.vue` + entrada de menú en `AppLayout` (sección Sistema). Crear cuenta envía invitación (token de reset) y **el enlace se devuelve a la vista** (`session('invite_url')`) para copiar/entregar en mano si el correo no llega — igual en `FamilyAccountController@invite`. Correo: `.env` pasa a SMTP de Gmail (`.env.example` documenta host/puerto/app-password); comando `mail:test {email}` para verificar. | Control de usuarios sin dependencias externas (se descartó Clerk por incompatibilidad con el stack) + correo real | Coordinación |
| 2026-09-06 | Portal familias / RBAC | Sin migraciones. i18n: traducidas al español las páginas de scaffolding de Breeze que seguían en inglés (`Pages/Auth/*`, `Pages/Profile/*`). `AppServiceProvider::boot()` sobreescribe en español los correos `ResetPassword` y `VerifyEmail` de Laravel. `Profile/Edit.vue` ahora elige layout según el rol (`PortalLayout` para familia, `AppLayout` para gestión); eliminado `Layouts/AuthenticatedLayout.vue` (Breeze, sin uso). Nuevo `CLAUDE.md` en la raíz con la regla "toda la UI en español" y guía de roles/portal. | La web tiene que ser 100% español | Cualquiera que regenere páginas de auth/perfil |
| 2026-09-06 | Portal familias / RBAC | Migración aditiva `create_family_user_table` (pivote N:M cuenta↔familia). **Rol nuevo `intendencia`** en `UserRole` (gestión de inventario; `canSeeAllBranches()` lo incluye). Permiso `settings.manage`→`settings.finance` (tesorería + admin); permisos nuevos `settings.finance` y `portal.access`. El rol `familia` pierde todos los permisos de módulo y solo tiene `portal.access`: lo que ve se acota en `App\Http\Controllers\Portal\*`. `User`: `families()`, `familyIds()`, `children()`, `childIds()`, `isFamilia()`, `isIntendente()`. `Family::users()`. Trait `App\Support\ResolvesBranchScope` (extraído de `MemberPolicy`); `MemberPolicy` y `Member::scopeVisibleTo()` acotan a `children()` para familias. Middleware global `RedirectByRole` (familia en `/dashboard`→`/portal`; gestión en `/portal/*`→`/dashboard`, solo GET). Alias de middleware de spatie (`role`/`permission`/`role_or_permission`) registrados en `bootstrap/app.php`. Portal de familias: `routes/features/portal.php` (`role:familia`), `Portal\{Dashboard,Payment,Calendar,Child}Controller`, `PortalLayout.vue` + `Pages/Portal/*`. `HandleInertiaRequests` comparte `auth.user.is_familia`/`is_intendente`; `useAuth` expone `isFamilia`/`isIntendente`. Alta de cuentas: `FamilyAccountController@invite/revoke` + botón en `Members/Families/Edit.vue` (envía enlace de contraseña). `DemoSeeder`: usuario `intendencia@grupo.test` y cuenta de portal `familia@grupo.test`. | Portal de cuentas para familias y control de roles por rama/función | Todos (nuevo rol; `familia` ya no pasa checks de permisos de módulo; `settings.manage` renombrado) |
| 2026-09-06 | Agente Panel | Sin migraciones. Sistema de diseño compartido aplicado a toda la plataforma: nuevos componentes `AppButton`, `StatCard`, `SectionCard`, `EmptyState`, `SearchInput`, `FilterSelect` y ampliación de `DashIcon`; `PageHeader` y `DataTable` reescritos (tabla con orden visible y modo tarjeta en móvil); `app.css` alinea `.btn-*`, `.card`, `.input` y `.label` con ese lenguaje; `tailwind.config.js` define `shadow-xs`/`shadow-2xs` (varias vistas ya las usaban sin que existieran). Barrido de 28 pantallas: icono de sección en la cabecera, KPIs con `StatCard`, filtros con `FilterSelect` y retirada de emojis de la interfaz. | Coherencia visual entre módulos | Todos los agentes de feature: usad estos componentes en vez de marcado propio |
| 2026-09-06 | Agente Panel | Sin migraciones. Rediseño del panel de inicio: `DashboardController` devuelve `attention` (avisos accionables con enlace y gravedad), `agenda` (fecha ISO), `finance` (pendiente/cobrado/ratio), `members` (censo por rama) e `inventory`; props antiguos `semaphore`/`upcomingEvents`/`recentCharges` eliminados. Nuevos componentes `DashIcon` y `CommandPalette` (Ctrl+K). `AppLayout`: barra superior con buscador y título de sección, barra lateral plegable recordada en localStorage, entradas nuevas de Familias y Asistencia. | Productividad y claridad del panel | Cualquier agente que dependiera de los props antiguos del Dashboard |
| 2026-09-06 | Agente Web pública | Sin migraciones. Capa de movimiento de la web pública: cabecera flotante que se contrae en píldora al bajar, se oculta al seguir bajando y vuelve al subir; enlace activo por sección visible; directiva `v-reveal` con variantes y cascada; contador animado en las cifras; acordeón de FAQ con altura animada; botón de volver arriba. `tailwind.config.js`: curva `ease-suave`, keyframes y animaciones. `app.css`: scroll suave y anulación global con `prefers-reduced-motion`. El contenedor público usa `overflow-x: clip` para que las entradas laterales no generen barra horizontal. | Interacción más fluida (petición del grupo) | Todos |
| 2026-09-06 | Agente Web pública | Sin migraciones. `vite.config.js`: bloque `server.cors` que autoriza `localhost`/`127.0.0.1` en cualquier puerto. Con Vite 6, laravel-vite-plugin limita el CORS del dev server al valor de `server.origin`, así que la app en `:8000` no podía cargar los assets de `:5173` y la web se veía en blanco. | Página en blanco en desarrollo | Todos (solo entorno de desarrollo) |
| 2026-09-06 | Agente Web pública | Sin migraciones. Nuevo sitio público: `config/group.php` (contenido editorial), `Public\HomeController` + `routes/features/site.php` (`/` → `Public/Home`), `PublicLayout` compartido por portada, `/galeria` e `/historia`, prop compartida `group` en `HandleInertiaRequests`, SEO + JSON-LD en `app.blade.php`, tokens `sand`/`forest` y fuente `display` en Tailwind. La antigua portada de la plataforma pasa a `/plataforma`. **Fix:** `FakeDriveService::createFolder` no cumplía la firma de la interfaz (rompía toda la suite de tests). | Web pública para familias nuevas | Todos (la raíz `/` ya no es la pantalla de bienvenida de la plataforma) |
| 2026-07-06 | Orquestador | Núcleo inicial (enums, migraciones §B, modelos, policies, DriveService, CampRatioValidator) | Base compartida | Todos |
| 2026-07-07 | Agente C | Migración aditiva `add_ical_token_to_users_table` (+`ical_token` en fillable de `User`) | Suscripción iCal de solo lectura al calendario de eventos (URL tokenizada por usuario) | Agente C (Calendario) |
| 2026-07-07 | Agente Seguridad | Migración `widen_health_card_number_on_health_records` (`health_card_number` varchar→text) + casts `encrypted` en `HealthRecord` (allergies, intolerances, medication, observations, health_card_number) + comando `health:encrypt-existing` (idempotente, cifra datos previos) + trait `LogsActivity` en Member/HealthRecord/Consent/LeaderProfile/ChargeMember/Document/Invoice (HealthRecord redacta valores médicos en el log) + middleware `SecurityHeaders` + scope Drive reducido a `drive.file` | Cifrado en reposo RGPD art. 9 y trazabilidad | Todos |
| 2026-07-07 | Agente Seguridad | `composer.json`: versiones `*` fijadas a `^` (dompdf ^3.1, breeze ^2.4, activitylog ^4.12, permission ^6.25). **Nota:** `config.policy.advisories.block` se mantiene en `false` a propósito — activarlo bloquea la instalación de Laravel 11 en este entorno (decisión previa del proyecto); revisar avisos de seguridad manualmente con `composer audit`. | Dependencias reproducibles | Todos |
| 2026-07-06 | Agente B (Tesorería) | Sin migraciones nuevas. Feature construido sobre el esquema existente (charges, charge_member, invoices, settings): cobros con reparto por rama/miembro y descuento hermanos, facturas + PDF vía DriveService, informe económico CSV, recordatorios (Mailable+Job+`charges:send-reminders`, no cableado en scheduler) y ajustes (`finance.*` keys en Setting) | Agente B |
